# Installation

To install the app you must download it from the github repository

    git clone https://github.com/SETaP-Team-5C/BookWorm-code-.git

Then you must create a virtual environment

    python -m venv venv

    source venv/bin/activate

Now install the requirements

    pip install -r requriements.txt